#!/usr/bin/make -f

CC=gcc-3.4
CXX=g++-3.4

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/class/autotools.mk

DEB_CONFIGURE_EXTRA_FLAGS := --enable-static

install/libvisual::
	find debian/libvisual -name '*.so' | xargs chrpath --delete
